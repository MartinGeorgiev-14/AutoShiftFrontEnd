import styled from "styled-components"


const Div = styled.div`
`

const Label = styled.label`

`
const Input = styled.input`
`

const InputDiv = ({ label }) => {
    <Div>
        <Label>Label</Label>
        <Input type="text" />
    </Div>
}

export default InputDiv